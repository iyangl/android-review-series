<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.lemayn.review"
          xmlns:tools="http://schemas.android.com/tools"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".aidl.ServiceActivity"/>
        <activity android:name=".cp.ContentProviderActivity"/>

        <service android:name=".aidl.LocalService"/>
        <service
            android:name=".aidl.RemoteService"
            android:process=":remote">
            <intent-filter>
                <action android:name="com.lemayn.review.aidl.remote"/>
            </intent-filter>
        </service>

        <provider
            android:name=".cp.RemoteProvider"
            android:authorities="com.lemayn.review.cp.RemoteProvider"
            android:permission="com.lemayn.review.provider"/>
    </application>

</manifest>